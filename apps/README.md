# apps

Applications for the Marib Tax System.

**Purpose:** Hosts the taxpayer mobile app (`mobile`), public/admin web app (`web`), NestJS API (`api`), and background worker (`worker`).

**Status:** Reserved directories only. No application frameworks have been initialized. Implementation has not started.
