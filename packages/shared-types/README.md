# packages/shared-types

Shared TypeScript types for the Marib Tax System.

**Purpose:** Cross-cutting type definitions shared by `apps/web`, `apps/api`, and `apps/worker`.

**Status:** Reserved directory only. Implementation has not started.
