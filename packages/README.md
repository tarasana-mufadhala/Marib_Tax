# packages

Shared Node.js packages for the Marib Tax System.

**Purpose:** Workspace packages consumed by `apps/web`, `apps/api`, and `apps/worker` via pnpm. Flutter (`apps/mobile`) is not part of the pnpm workspace.

**Status:** Reserved packages only. Package implementation has not started.
