# تقرير مطابقة النظام لمعايير مستندات work-system

**التاريخ:** 2026-08-07 (آخر تحديث)
**المرجع المعتمد:** `تحليل.md` (المرجع الوحيد للتنفيذ والاختبار والتسليم) + بقية مستندات المجلد (باستثناء «ما تم إنجازه وما هو متبقي» لقدمه).
**نطاق الفحص:** لوحة تحكم الأدمن + الـ API + قاعدة بيانات Supabase + الموقع الإلكتروني العام.

---

## أولاً: ما تحقق ويعمل فعلياً (مُختبَر حياً من المتصفح والـ API)

| البند في المستند | الحالة | الدليل |
|---|---|---|
| 6.6 قائمة الطلبات والبلاغات من قاعدة البيانات | ✅ يعمل | `/api/v1/admin/requests` مع اسم المكلف ونوع الخدمة |
| تحديث حالة الطلب يدوياً (بما فيه «بانتظار السداد») | ✅ يعمل | `PATCH /admin/requests/:id/status`، وحالة `payment_required` تظهر «بانتظار السداد» كما في البند 4.6 |
| 6.6 تفاصيل الطلب وسجل الحالات | ✅ يعمل | `GET /admin/requests/:id/details` — بيانات الطلب كاملة + سجل الحالات من `requests.request_status_histories`، مع مودال «التفاصيل 📄» في صفحة الطلبات |
| 6.6 مرفقات الطلبات ورفع نموذج الموافقة/الرفض | ✅ تعمل — مُختبَرة حياً | `POST /admin/requests/:id/attachments` يرفع إلى Supabase Storage (حاوية `admin-attachments`) ويسجل في `files.attachments` + `files.attachment_links` مع سجل تدقيق؛ `GET /admin/requests/:id/attachments` للسرد؛ `GET /admin/attachments/:id/file` للفتح/التحميل (تحقق: رُفع PDF وحُمّل بنجاح بنفس الحجم 1438 بايت). قسم «المرفقات والنماذج 📎» في مودال التفاصيل (مرفق عام / نموذج موافقة / نموذج رفض). الأنواع المسموحة مطابقة لقيود الحاوية: PDF، PNG/JPEG، docx، xlsx، CSV — حتى 10MB |
| 9 تخزين الملفات (Supabase Storage) | ✅ مدمج | `StorageService` في الـ API يرفع/يحمّل عبر واجهة Supabase Storage بمفتاح service role. الحاويات جاهزة: `admin-attachments` (خاصة، 10MB)، `taxpayer-documents` (خاصة، 5MB)، `public-forms` (عامة، 5MB). ملفات الاستيراد تُحفظ الآن فعلياً (`admin-attachments/imports/{jobId}/...`) مع `GET /admin/imports/:id/file` — تحقق: الملف المحمّل مطابق للأصل بايت-ببايت |
| 6.6 سجل المكلفين من قاعدة البيانات | ✅ يعمل | `/api/v1/admin/taxpayers` |
| جدولة/سرد النزول الميداني | ✅ يعمل (سرد) | `/api/v1/admin/visits` |
| المستحقات: تسجيل المبالغ والسداد يدوياً (نطاق MVP) | ✅ يعمل | `/admin/dues` + تسجيل سداد + بطاقات «بانتظار السداد: العدد وإجمالي المبالغ» كما في 6.7 |
| 6.7 لوحة المؤشرات التنفيذية (أرقام حقيقية) | ✅ يعمل | `/reports/executive-summary` من القاعدة |
| 6.7 كتالوج التقارير الرقابية الـ 29 | ✅ مكتمل | `reports-catalog.ts` يعرض الـ 29 تقريراً الرسمية (1-3 من تحليل.md و4-29 من مستند التقارير) مع صلاحيات الأدوار لكل تقرير |
| 6.7 توليد التقارير ببيانات حية | ✅ 29 من 29 | `GET /api/v1/reports/:reportId` باستعلامات SQL حقيقية لكل التقارير الـ29 — كلها مُختبَرة حياً وتعيد `available:true` |
| **التقارير الستة التي كانت بلا جداول** | ✅ اكتملت — مُختبَرة حياً | **REP-18 (OTP)** و**REP-27 (الدخول والأمان)** من جدول `identity.auth_events` الجديد: `OtpService` يسجّل `otp_requested/verified/failed/expired/rate_limited` و`AuthnService` يسجّل `login_success/failed/locked/blocked`، والهواتف تُعرض مُقنّعة (`+9677****56`) ولا يُسجَّل أي رمز أو كلمة مرور. **REP-29 (استخدام الموقع)** من `content.page_views` عبر `POST /public/page-views` ومكوّن `PageViewTracker` في الموقع (بلا IP ولا معرّف زائر — عدّاد صفحات فقط). **REP-20** من طلبات النواقص ومرفقات نماذج الرفض. **REP-24 (جودة البيانات)** استعلام تحليلي بلا جدول جديد: 11 فحصاً في ثلاث فئات (تكرار / سجلات يتيمة / حالات غير منطقية). **REP-26 (العمليات الحساسة)** يوحّد ثلاثة مصادر حقيقية: `audit.audit_logs` المصنّفة، وقرارات الرفض من `request_decision_records`، وإسناد/سحب الأدوار من `staff_role_assignments`. تحقق حي: الستة رجعت `available:true` بأرقام حقيقية (REP-24 التقط خللاً فعلياً: «مستحق مسدد بلا إيصال سداد = 1»، وREP-27 عرض 5 محاولات فاشلة وقفل حساب واحد من اختبار دخول حي، وREP-29 عدّ 4 مشاهدات موزّعة على 3 صفحات) |
| **مصدر الجداول التي أُنشئت خارج الهجرات** | ✅ طُبّقت وسُجّلت | `supabase/migrations/20260806000000_create_operational_telemetry_tables.sql` تُصدر كمصدر موثّق الجداول الثلاثة التي أُنشئت مباشرة على القاعدة (`identity.auth_events`، `content.page_views`، `content.contact_messages`)، وتضيف فهارسها، وتُفعّل RLS بسياسات مطابقة لنمط الدفعة 16. طُبّقت على staging بمعاملة واحدة وسُجّلت في `supabase_migrations.schema_migrations` (20 هجرة الآن)؛ Supabase CLI تعذّر اتصاله (timeout على 5432) فطُبّقت عبر عميل pg بنفس الأثر. تحقق بعد التطبيق: الثلاثة `rls=true` بسياسة لكلٍّ، والـ API يقرأ ويكتب فيها بلا تأثر (يتصل بدور المالك) |
| **🔐 حماية نقاط الـ API الإدارية** | ✅ أُعيدت — مُختبَرة حياً | أُزيل `@PublicEndpoint()` عن `AdminController` و`ImportsController` و`AttachmentsController` و`ReportsController` (كانت 40+ نقطة مكشوفة بلا مصادقة، منها سجل التدقيق REP-25)، وأُسندت صلاحية لكل نقطة من كتالوج العقود. التقارير الأمنية (REP-25/26/27) تتطلب `audit.sensitive.view` فوق `report.view` مطابقةً لـ allowedRoles. تحقق حي: 20 نقطة ترجع **200 برمز المدير و401 بلا رمز**، وموظف بصلاحية `report.view` وحدها رجعت له REP-01 بـ200 وREP-26/27 و`/admin/users` و`/admin/requests` بـ403. نقاط الموقع العام (`/public/*`) بقيت مفتوحة عمداً. مثبّت في `test/reports.authz.test.ts` (6 اختبارات تشمل حالات الرفض) |
| **دخول الموظفين** | ✅ يعمل — مُختبَر حياً | مزود الهاتف معطّل في Supabase (`phone_provider_disabled` — عُزل بنداء مباشر على GoTrue)، بينما **الدخول بالبريد يعمل**. أُضيف `POST /api/v1/auth/login/email` يشارك منطق القفل وتسجيل الأحداث مع مسار الهاتف، ومسار الهاتف باقٍ كما هو ويعمل فور تفعيل المزود. سكربت البذر يمنح الحساب الإداري بريداً (`admin@marib-tax.gov.ye`) ويُصحّح خللاً كان يمنعه من إيجاد المستخدم القائم (GoTrue يخزّن الهاتف بلا `+`). اللوحة: صفحة الدخول تطلب البريد للموظفين والهاتف للمكلفين، والترويسة تعرض المستخدم الحقيقي من `GET /admin/me` بدل الاسم المثبّت في الكود |
| **إصلاح فخّ ابتلاع أخطاء في مسار التفويض** | ✅ أُصلح مع اختباراته | `actor-profile.repository.ts` كان يلفّ حلّ الصلاحيات بـ `try/catch` صامت: أي عطل قاعدة عابر كان يُترجم إلى **401 «غير مصادق» مضلّل**، وعطل أثناء البحث عن ملف الموظف كان **يُهبط موظفاً إلى صلاحيات مكلف بصمت**. الآن يُميَّز «لا يوجد حساب» (NotFoundException ⇒ رفض) عن عطل البنية التحتية (يُسجَّل ويُرفع). 6 اختبارات في `test/actor-profile.repository.test.ts` |
| 6.1/6.2/6.3 إدارة الخدمات والكيانات القانونية | ✅ تعمل | صفحة «إدارة الخدمات والكيانات» في اللوحة: `GET /admin/services` + `PATCH /admin/services/:id/toggle` + `GET/POST /admin/legal-entities` مع مودال إضافة كيان |
| 6.10 عرض المستخدمين والأدوار | ✅ يعمل | `/admin/users` من `identity.staff_profiles` + الأدوار — أُزيلت البيانات الوهمية |
| 6.10 إضافة موظف جديد | ✅ مبني (يحتاج تفعيل الهاتف) | `POST /admin/users` ينشئ حساب GoTrue + user_profile + staff_profile (رمز تلقائي EMP-XXXXXX) + إسناد دور؛ مودال الإضافة في صفحة المستخدمين مع جلب الأدوار من `GET /admin/roles` |
| 6.4 إدارة صفحات المحتوى الثابتة | ✅ تعمل | قسم «صفحات الموقع الثابتة» في صفحة المحتوى: `GET/POST /admin/content-pages` (upsert بمفتاح الصفحة، حالة published) للمفاتيح about/contact/guidelines/info-center |
| سجل القرارات المرتبطة بالطلبات | ✅ يعمل | `/admin/decisions` من `request_decision_records` |
| 6.5 نشر إعلان على الموقع العام | ✅ يعمل | نموذج النشر يحفظ في `content.announcements` (اختُبر من المتصفح) |
| 6.4 مكتبة النماذج والقوانين | ✅ تعمل — مُختبَرة حياً | `POST /admin/library-documents` يرفع PDF/PNG/JPEG (حتى 5MB) إلى حاوية `public-forms` العامة ويسجل في `content.library_documents` (مسودة أولاً)؛ `PATCH /admin/library-documents/:id/toggle` للنشر/إيقاف النشر مع `published_at`؛ `GET .../:id/file` للفتح (تحقق: الملف المحمّل مطابق للأصل بايت-ببايت). قسم «مكتبة النماذج والقوانين» في صفحة المحتوى بتصنيفات: نموذج / قانون / دليل / **قرار** (أُضيف «قرار» ليطابق البند 6.4)، مع سجل تدقيق `library.uploaded/published/unpublished` |
| 6.8 سجل عمليات الاستيراد | ✅ يعمل | `/admin/imports` من `imports.import_jobs` مع عدّادات الصفوف (إجمالي/صحيحة/مرفوضة) |
| 6.8 الاستيراد على مرحلتين (معاينة وتحقق → اعتماد وترحيل) | ✅ يعمل — مُختبَر حياً | `POST /admin/imports/upload` (Excel/CSV حتى 5MB و5000 صف) + `GET /admin/imports/:id` + `POST /admin/imports/:id/approve` + `reject`. قالبا: مكلفون وأنشطة تجارية. التحقق: الحقول الإلزامية، توحيد صيغ الأرقام (أرقام عربية ← لاتينية، إزالة 967+)، منع تكرار الهاتف/الرقم الضريبي داخل الملف ومقابل القاعدة (`registry.taxpayer_contacts`)، وفحص التكرار مجدداً لحظة الترحيل. الترحيل داخل معاملة واحدة: إنشاء `registry.taxpayers` + جهات الاتصال أو `masterdata.commercial_activities`، مع سجل تدقيق `import.uploaded/approved/rejected`. الترحيل لا يتم إلا بضغط اعتماد، والاعتماد المزدوج محمي |
| الواجهة العربية RTL سليمة، بلا نصوص إنجليزية غير لازمة | ✅ محسّن | الحالات والتواريخ تُعرض بالعربية (`بانتظار السداد`، `2026/8/4`) بدل الرموز الخام |
| **6.8 قالب استيراد المستحقات (ثالث القوالب)** | ✅ يعمل — مُختبَر حياً | قرار الربط المحسوم: `payment_dues` لا يحمل `taxpayer_id`، لذا يُربط كل مستحق بمرجع الطلب مباشرة إن حُدد، وإلا بأحدث طلب للمكلف (عبر الهاتف/الرقم الضريبي في `taxpayer_contacts`)؛ مكلف بلا طلب يُتخطى بصدق دون إنشاء مستحق يتيم. الأعمدة: مرجع الطلب أو هاتف/رقم المكلف + المبلغ (إلزامي، رقم موجب) + العملة (اختياري، افتراضي YER). التحقق يرفض: مرجع طلب غير موجود، مكلف غير موجود، مبلغ غير صالح. الحالة الافتراضية `unpaid` تظهر «غير مسدد» ضمن بانتظار السداد. تحقق حي (ملف `test-dues.csv` بخمس حالات): صفّان صحيحان وثلاثة مرفوضة برسائل دقيقة، والاعتماد رحّل `DUE-AEEF19` (75,000 ر.ي مربوط بـ REQ-TEST-001) وتخطى مكلفاً بلا طلب، والاعتماد المزدوج محمي |
| الأمان: JWT + صلاحيات + عزل المكلفين | ✅ يعمل | تحقق HS256/JWKS هجين، صلاحيات لكل نقطة محروسة، لا كشف للبيانات بدون رمز |
| حالات الطلبات وفق البند 4.6 | ✅ مطابقة | `payment_required → بانتظار السداد` وبقية التعيينات موثقة في `api-client.ts` |
| **5 نقاط API العامة للمحتوى المنشور** | ✅ تعمل — مُختبَرة حياً | `PublicContentController` على `/api/v1/public` بلا مصادقة (`@PublicEndpoint`): `GET /public/announcements` (النشطة مع فلترة starts_at/ends_at)، `GET /public/services` (النشطة)، `GET /public/content-pages/:key` (المنشورة فقط)، `GET /public/library-documents?category=` (المنشورة فقط، بلا file_path)، `GET /public/library-documents/:id/file` (تحميل عبر التخزين، يرفض غير المنشور 404). تحقق حي: إعلان + خدمة + صفحتا محتوى + مستند منشور كلها رجعت ببياناتها، والمسودات لا تظهر، وتحميل الملف 200 بنفس الحجم 1426 بايت |
| **5.1 الرئيسية بالبيانات الحية** | ✅ تعمل | تجلب الإعلانات والخدمات من الـ API العام عند التحميل (تحقق بصري: الإعلان التجريبي وخدمة TEST_SVC ظهرا في DOM المتصفح) مع حالات فارغة صادقة، والتصميم والحركات محفوظة |
| **5.2 النماذج والقوانين والقرارات** | ✅ تعمل | صفحات forms/laws/decisions تجلب من مكتبة المستندات المنشورة حسب التصنيف (form/law/decision) مع روابط تحميل/فتح الملف الفعلية. تحقق: صفحة القوانين تعرض «قانون الضريبة التجريبي 2026» المنشور، وصفحتا النماذج والقرارات تعرضان حالة فارغة صادقة لعدم وجود منشور من تصنيفيهما |
| **5.3 الخدمات والإرشادات وعن المكتب والتواصل** | ✅ تعمل | الخدمات من `service_types` النشطة؛ «عن المكتب» و«الإرشادات» و«التواصل» تعرض محتوى `content_pages` المنشور (about/guidelines/info-center/contact) مقسوماً لفقرات، مع نص مؤسسي افتراضي عند غياب المحتوى |
| **5.4 صفحة تحميل التطبيق** | ✅ ثابتة بقصد | معلومات التطبيق وزر التنزيل كما يقتضي المستند (صفحة تعريفية ثابتة) |
| **قناة استقبال رسائل التواصل (مكتملة طرفاً لطرف)** | ✅ تعمل — مُختبَرة حياً | جدول `content.contact_messages` (اسم/هاتف/بريد/رسالة/حالة). النموذج في الموقع العام يُرسل فعلياً إلى `POST /public/contact-messages` مع تحقق إلزامية الحقول وحدود الطول، ورسالة نجاح/خطأ صادقة للزائر. اللوحة: قسم «رسائل التواصل الواردة» في صفحة المحتوى عبر `GET /admin/contact-messages` + `PATCH /admin/contact-messages/:id/read` (شارة جديدة/مقروءة + زر تعليم). تحقق حي: أُرسلت رسالة عبر curl ورسالة عبر نموذج المتصفح الفعلي، وظهرتا في اللوحة «(1 جديدة من 2)»، وتعليم المقروءة يعمل |

## ثانياً: فجوات متبقية مقابل المستند (مرتبة بالأولوية)

### أ) فجوات لوحة التحكم
1. **تفعيل مزود الهاتف في Supabase** — الدخول بالهاتف معطّل (`phone_provider_disabled`)، فيعمل الموظفون بالبريد حالياً. تفعيله مطلوب لتطبيق الجوال (دخول المكلفين برقم الهاتف وOTP) ولإنشاء الموظفين بالهاتف. يتطلب عادةً ربط مزود SMS (Twilio) أولاً — راجع البند 4.
2. ~~أكواد صلاحيات ناقصة في العقود~~ — **أُغلقت.** أُضيفت السبعة المطلوبة إلى `packages/contracts/src/index.ts` (`user.read`, `user.manage`, `role.read`, `role.assign`, `masterdata.manage`, `attachment.read`, `attachment.upload`) وبُنيت في dist، واستُبدلت بها التعليمات المؤقتة في وحدات التحكم، ومُنحت لدور `office_manager` (51 صلاحية). تحقق حي بعد الاعتماد: موظف يملك `report.view + request.read` فقط رجعت له 200 على REP-01 و`/admin/requests` و`/admin/services`، و403 على REP-26/27 و`/admin/users` و`/admin/roles` و`/admin/dues` و`/admin/content-pages` و`/admin/imports`.

### ب) فجوات على مستوى النظام (خارج نطاق ما تم في هذه الجولة)
3. **تطبيق الجوال (المكلف)** — الخدمات الخمس FR-101..105 والبلاغات الستة FR-201..206 لم تُبنَ (القسم 4 كاملاً).
4. **Twilio (SMS/WhatsApp)** — القناة الوحيدة المعتمدة للتحقق والإشعارات (القسم 3)؛ الإشعارات حالياً Mock في `OtpService`. مطلوب: دمج Twilio + تسجيل حالة كل رسالة وإعادة المحاولة (معيار القبول).
5. **تدقيق شامل + نسخ احتياطي موثق** — سجل التدقيق يغطي الآن الاستيراد والمرفقات والمكتبة إضافة إلى authz؛ النسخ الاحتياطي اليومي غير موثق بعد (7 + معايير القبول).

## ثالثاً: ملاحظات بيانات الاختبار الموجودة حالياً في القاعدة
- مكلف/طلب/مستحق تجريبي: `TIN-TEST-001` / `REQ-TEST-001` / 150,000 ر.ي (سُجّل سداده أثناء الاختبار).
- إعلان تجريبي منشور: «بدء فترة تقديم الإقرارات الضريبية لعام 2026».
- خدمة تجريبية `TEST_SVC` في `requests.service_types`.
- بيانات اختبار الاستيراد: 3 مكلفين (`TIN-BEF778`، `TIN-E85EE2`، `TIN-BC6705`) مع هواتفهم وأرقامهم الضريبية، ونشاطان تجاريان (`ACT-B3206A`، `ACT-662A53`)، و5 عمليات استيراد (آخرها مستحقات). ملفات الاختبار: `work-system/test-taxpayers.csv` و`work-system/test-activities.csv` و`work-system/test-dues.csv`.
- مستحق تجريبي مستورد: `DUE-AEEF19` بمبلغ 75,000 ر.ي (غير مسدد) مربوط بالطلب `REQ-TEST-001` — نتج عن اختبار قالب استيراد المستحقات.
- مرفق تجريبي: `test-approval-form.pdf` مرتبط بالطلب `REQ-TEST-001` في حاوية `admin-attachments` (مساره في `files.attachments`).
- مستند مكتبة تجريبي: «قانون الضريبة التجريبي 2026» — **منشور الآن** (نُشر لاختبار صفحة القوانين) في حاوية `public-forms`، ملفه `work-system/test-law.pdf`.
- صفحتا محتوى تجريبيتان منشورتان: `about` (نبذة تأسيسية) و`guidelines` (دليل المكلف الجديد) في `content.content_pages`.
- رسالتا تواصل تجريبيتان في `content.contact_messages`: «مكلف تجريبي للتواصل» (مقروءة) و«زائر تجريبي» (جديدة — أُرسلت من نموذج الموقع الفعلي).
- لقطات تحقق الموقع العام: `work-system/verify-website-1.png` (الرئيسية/الهيرو)، `verify-website-2.png` (الإعلانات والخدمات الحية)، `verify-website-3.png` (صفحة القوانين بمستند منشور)، `verify-contact-messages.png` (قسم رسائل التواصل في اللوحة).
يُنصح بحذفها قبل التسليم أو استبدالها ببيانات حقيقية.
