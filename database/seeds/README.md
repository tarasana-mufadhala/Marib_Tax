# database/seeds

Database seed data for the Marib Tax System.

**Purpose:** Non-production seed scripts for local and staging environments only. Production data must never be used as seed input.

**Status:** Reserved directory only. Implementation has not started.
